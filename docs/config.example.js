/**
 * 이 파일을 config.js 로 복사한 뒤 값을 채우세요.
 * config.js 는 .gitignore 에 넣어 비밀번호가 공개 저장소에 안 올라가게 하세요.
 */
window.INTAKE_CONFIG = {
  /** Vercel 배포 후 API 주소 (예: https://global-branding-xxx.vercel.app/api/submit) */
  submitUrl: '',

  /** Vercel 환경변수 SUBMIT_SECRET 과 동일 (선택, 스팸 방지) */
  submitSecret: '',
};
